{
  "name": "mirit-nails-functions",
  "description": "Server-side functions for mirit-nails — currently: Invoice4U receipt issuance.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth",
    "test": "node --test test/"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.1"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.4.0"
  }
}
